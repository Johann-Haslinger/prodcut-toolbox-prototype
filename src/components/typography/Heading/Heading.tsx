import styled from "styled-components";
import tw from "twin.macro";

const Heading = styled.p`
  ${tw`text-4xl text-center font-black leading-[48px] text-[#1A1B1D] dark:text-white`};

`;

export default Heading;
